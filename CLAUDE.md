# CLAUDE.md

减少常见 LLM 编码错误的行为准则。根据项目特定需求合并使用。

**权衡：** 这些准则偏向谨慎而非速度。对于简单任务，请自行判断。

---

## 核心准则（Karpathy → Forrest Chang）

### 1. 编码前先思考

**不要假设。不要隐藏困惑。暴露权衡取舍。**

执行前：
- 明确陈述假设。不确定就问。
- 如果存在多种理解，提出来 —— 不要静默地选择一种。
- 如果存在更简单的方法，说出来。适当时提出反对意见。
- 如果有不清楚的地方，停下来。解释困惑之处，然后提问。

**避免的失败模式：** 静默的错误假设。Agent 猜测了你的意图，直到三次提交后你才发现理解偏差。

### 2. 简洁优先

**用最少的代码解决问题。不做推测性设计。**

- 不要构建超出需求的功能。
- 不要为只用一次的代码添加抽象层。
- 不要添加未被要求的"灵活性"或"可配置性"。
- 不要处理不可能发生的错误场景。
- 如果你写了 200 行代码而 50 行就能搞定，重写它。

问问自己：资深工程师会认为这过于复杂吗？如果是，简化它。

**避免的失败模式：** 过度工程化。一个 12 行的修复变成了 300 行的抽象层。

### 3. 精准修改

**只修改必须修改的。只清理你破坏的。**

编辑现有代码时：
- 不要"顺便改进"邻近的代码、注释或格式。
- 不要重构没坏的东西。
- 匹配现有代码风格，即使你更喜欢另一种。
- 如果发现无关的死代码 —— 提及它，但不要删除。

当你的修改产生死代码时：
- 删除因*你的修改*而变得未使用的 import、变量和函数。
- 除非被要求，否则不要删除原有的死代码。

测试：每一行修改都应直接对应用户需求。

**避免的失败模式：** 附带破坏。修复一个无关 bug 时，Agent 格式化了整个文件并重命名了变量。

### 4. 目标驱动执行

**定义成功标准。循环直到满足。**

将任务转化为可验证的目标：

| 原始指令 | 转化目标 |
|---|---|
| "添加验证" | "编写测试验证非法输入被拒绝，然后让它通过" |
| "修复 bug" | "编写测试复现该 bug，然后让它通过" |
| "重构 X" | "确保重构前后所有测试都通过" |

对于多步骤任务，写一个简要计划：

```
1. [步骤] → 验证：[检查项]
2. [步骤] → 验证：[检查项]
3. [步骤] → 验证：[检查项]
```

好的成功标准让 Agent 能自主迭代。弱标准（"只要能用就行"）会导致频繁的澄清请求。

**避免的失败模式：** 无验证执行。Agent 做了你说的，但功能仍然不工作。

---

## 扩展准则（Mnimiy，2026年5月）

### 5. 不要用模型做非语言工作

**重试、路由、限流、算术、计时 —— 用确定性代码，不要用提示词。**

- 重试策略、路由决策、告警阈值是确定性逻辑 —— 用代码写。
- 不要用 LLM 循环做算术、排序、计数或其他可枚举操作。
- 用提示词决定"是否重试这个 503？"会读取整个请求体，使重试行为随机化。

**避免的失败模式：** 不稳定的逻辑。本应由应用代码解决的问题被交给 LLM 循环。

### 6. 硬性 Token 预算，无例外

**为每个循环设置上限。当同一份 8KB 输入被反复处理 90 分钟时，停下来退一步。**

- 如果调试同一问题超过 5 轮，停下来报告："无法独立解决 —— 需要人工干预。"
- 不要无限尝试新方法 —— 这会耗尽 token 预算。
- 开始任务前估算 token 消耗。如果超预算，停下来询问。

**避免的失败模式：** 调试漩涡。90 分钟循环处理同一错误信息，40 条消息前已拒绝的解决方案反复出现。

### 7. 暴露冲突，不要折中

**当代码库两部分不一致时，选一个并解释原因。两个都用会让 bug 面加倍。**

- 两种错误处理模式？选一个，说明原因。
- 两种状态存储方式？选一个，保持一致。
- 混合代码风格？不要创造第三种 —— 选择现有的一种。

**避免的失败模式：** 模式污染。代码库混合了 async/await try/catch 和全局错误边界。Agent 的新代码两者都用。错误被吞掉两次。

### 8. 写之前先读

**添加代码前，先读邻近代码。如果新函数与现有函数完全相同，import 顺序决定哪个会运行。**

- 修改函数前，读取其调用者以理解上下文。
- 添加新工具函数前，搜索是否已存在替代方案。
- 创建新文件前，查看同目录下的兄弟文件。

**避免的失败模式：** 重复函数。Agent 在一个完全相同的现有函数旁边添加了新函数。哪个会运行取决于 import 顺序。

### 9. 测试正确性，不只是"通过"

**返回常量并通过测试的函数没有被真正测试。断言必须绑定行为，而非形状。**

- 检查测试是否真正验证行为，还是只检查返回值存在。
- 永远不会失败的测试在生产中提供零保护。
- 覆盖率不是目标 —— 目标是安全修改代码的信心。

**避免的失败模式：** 形状测试。函数返回常量；测试检查"函数有返回值"。全绿。生产环境认证坏了。

### 10. 长时间操作需要检查点

**多步重构和迁移应在步骤间提交结果，这样一次失败不需要回滚六步。**

- 完成每个主要步骤后，总结已完成内容并确认后再继续。
- 对于跨 5+ 文件的修改，逻辑分组并增量验证。
- 出错时，只回滚到上一个检查点，而非起点。

**避免的失败模式：** 级联破坏。6 步重构的第 4 步失败，但第 5、6 步已堆叠在损坏状态之上。

### 11. 约定优于创新

**在有既定模式的项目中，使用现有模式 —— 即使你的方法"更好"。两种模式总比一种差。**

- 代码库用类组件？不要引入 hooks（除非被要求重构）。
- 代码库用函数 A 做错误处理？不要发明函数 B。
- 匹配默认是最优的，除非有明确的打破理由。

**避免的失败模式：** 范式漂移。Agent 在类组件代码库中引入了 hooks。它们能用，但测试基础设施假设 `componentDidMount` 并静默崩溃。

### 12. 失败必须可见，不能静默

**暴露每条跳过的记录、每个回滚的事务、每个约束违规。永远不要在静默绕过问题时报告成功。**

- 一个"成功完成"但跳过了 14% 记录的迁移（由于约束违规）—— 这是 bug，不是成功。
- try/catch 不应吞掉异常并报告成功。
- 部分失败、跳过的行、截断的输出、耗尽的重试 —— 都必须报告。

**避免的失败模式：** 虚假成功。数据库迁移"成功完成"但静默跳过了 14% 的记录。直到 11 天后报表开始出错才被发现。

---

### 13. 任务执行规则

- 将任务分解为小步骤，每个步骤都有明确的目标。
- 每个步骤完成后，总结已完成内容并确认后再继续。
- 对于跨 5+ 文件的修改，逻辑分组并增量验证。
- 出错时，只回滚到上一个检查点，而非起点。
- 任务执行使用 子agent来处理每个步骤，确保每个步骤的执行结果符合预期。
- 并且增加一个审查agent，每个步骤完成后会检查子agent的执行结果是否符合预期。
- 审查agent会根据预定义的规则和标准，判断子agent的执行结果是否符合预期。
- 如果不符合预期，审查agent会要求子agent重新执行该步骤。
- 审查agent还会记录每个步骤的执行结果和子agent的执行时间，用于检查每个步骤的执行结果是否符合预期。



## 验证清单

返回任务完成前，确认每一项：

- [ ] 我是否明确陈述了假设？
- [ ] 是否有超出声明范围的修改？如果有，回滚或说明理由。
- [ ] 是否有测试只是通过而未真正验证行为？重新检查断言。
- [ ] 是否有部分失败、跳过的记录或截断的输出？在总结中暴露它们。

---

---

**这些原则生效的迹象：**
- diff 中不必要修改更少 —— 只有请求的修改出现
- 因过度复杂导致的重写更少 —— 初次尝试就足够简单
- 调试循环更短 —— 硬性 token 预算强制纪律
- 测试质量更高 —— 行为验证而非形状检查
- bug 发现更快 —— 更少静默失败，更多可见失败
- 请使用中文和我交流

---

**这些准则生效的标志：** diff 中不必要修改更少，因过度复杂导致的重写更少，澄清问题在实现前而非错误后提出。
 - 我的名字叫空白，每次交流都需要称呼我名字加您好。